# 🧪 E2E Testing Suite - rus100 Project

## ✨ Обзор

Полная система E2E тестирования с BDD подходом, визуальной регрессией и автоматизацией для проекта rus100.

**Статус:** ✅ Production Ready  
**Версия:** 1.0.0  
**Последнее обновление:** 2025-10-09

---

## 🚀 Быстрый старт

### 1. Установка
```bash
npm install
npm run test:e2e:install  # Установка браузеров Playwright
```

### 2. Запуск тестов
```bash
# BDD тесты (рекомендуется)
npm run test:bdd

# Обычные E2E тесты
npm run test:e2e

# Интерактивный режим
npm run test:bdd:ui
```

### 3. Просмотр результатов
```bash
npm run test:bdd:report
```

---

## 📁 Структура

```
tests/e2e/
├── 📄 teacher-workflow.feature      # BDD сценарии (работа преподавателя)
├── 📄 test-management.feature       # BDD сценарии (управление тестами)
│
├── 📝 steps.ts                      # Ручные step definitions (82 шага)
├── 🤖 generated-steps.ts            # Автогенерируемые шаги (из компонентов)
│
├── 🎨 visual-regression.spec.ts     # Визуальное сравнение
├── 🎯 design-tokens.spec.ts         # Тесты дизайн-токенов (автоген)
│
├── 📚 page-objects/                 # Page Object Model классы
│   └── BasePage.ts
│
├── 📖 README.md                     # Этот файл
├── 📋 IMPLEMENTATION-COMPLETE.md    # Статус реализации
├── 🏗️ REFACTORING-COMPLETED.md     # История рефакторинга
└── ⚙️ .gitignore                    # Игнорируемые файлы
```

---

## 🎯 Реализованные сценарии

### teacher-workflow.feature (6 сценариев)

1. **Просмотр активных групп** `@критичный @навигация`
   - Просмотр списка групп
   - Отображение количества студентов
   - Отображение количества тестов
   - Прогресс обучения

2. **Открытие карточки группы** `@критичный @группы`
   - Детальная информация о группе
   - Список студентов
   - Статистика по тестам

3. **Просмотр прогресса студента** `@студенты`
   - Карточка студента
   - Общий прогресс
   - Результаты тестов
   - Тесты в процессе

4. **Навигация с клавиатуры** `@доступность @keyboard`
   - Tab-навигация
   - Визуальные индикаторы фокуса
   - Доступность элементов

5. **Переключение темной темы** `@темы @ui`
   - Переключение темы
   - Обновление цветов
   - Сохранение состояния

### test-management.feature (7 сценариев)

1. **Создание нового теста** `@критичный @создание-тестов`
2. **Редактирование существующего теста** `@редактирование`
3. **Предварительный просмотр теста** `@preview @валидация`
4. **Назначение теста группе** `@назначение`
5. **Просмотр статистики по тесту** `@статистика`
6. **Копирование теста** `@копирование`

**Итого: 13 бизнес-сценариев, 82 step definitions**

---

## 📚 Команды

### Основные команды

```bash
# E2E тесты
npm run test:e2e              # Обычные Playwright тесты
npm run test:e2e:ui           # UI mode (интерактивный)
npm run test:e2e:headed       # С браузером
npm run test:e2e:debug        # Debug mode

# BDD тесты (Gherkin)
npm run test:bdd              # Запуск BDD сценариев
npm run test:bdd:headed       # BDD с браузером
npm run test:bdd:ui           # BDD в UI mode

# Специализированные
npm run test:tokens           # Тесты дизайн-токенов
npm run test:visual           # Visual regression
npm run test:all              # Все тесты сразу

# Отчеты
npm run test:e2e:report       # Открыть HTML отчет E2E
npm run test:bdd:report       # Открыть HTML отчет BDD
```

### Автоматизация

```bash
# Обновить step definitions из компонентов
npm run update:tests

# Синхронизировать дизайн-токены
npm run sync:tokens

# Полная подготовка
npm run prepare:tests
```

---

## 🎨 BDD подход

### Написание сценариев

```gherkin
# teacher-workflow.feature
Сценарий: Преподаватель просматривает группу
  Допустим я открыл приложение генератора тестов
  И страница полностью загружена
  Когда я просматриваю список групп
  Тогда я вижу группу "Русский-Годовой, 2025"
  И группа отображает количество студентов
```

### Реализация шагов

```typescript
// steps.ts
Given('я открыл приложение генератора тестов', async ({ page }) => {
  await page.goto('/');
  await page.waitForLoadState('networkidle');
});

Then('я вижу группу {string}', async ({ page }, groupName: string) => {
  const groupCard = page.locator(`text="${groupName}"`);
  await expect(groupCard).toBeVisible();
});
```

### Запуск конкретного сценария

```bash
# Только критичные тесты
npx playwright test --grep @критичный

# Только навигация
npx playwright test --grep @навигация

# Конкретный feature файл
npx playwright test teacher-workflow.feature
```

---

## 🤖 Автоматизация

### 1. Автогенерация step definitions

При добавлении `data-testid` в компонент:

```tsx
// src/components/SubmitButton.tsx
<button data-testid="submit-button">
  Отправить
</button>
```

Запустите:
```bash
npm run update:tests
```

Автоматически создается в `generated-steps.ts`:
```typescript
Given('элемент submit button отображается', async ({ page }) => {
  await expect(page.locator('[data-testid="submit-button"]')).toBeVisible();
});

When('пользователь кликает на submit button', async ({ page }) => {
  await page.locator('[data-testid="submit-button"]').click();
});
```

### 2. Git Hooks

При коммите автоматически:
```bash
git commit -m "feat: add new component"
# → Pre-commit hook запускает update:tests
# → Регенерируется generated-steps.ts
# → Обновляется bddgen
```

### 3. CI/CD

GitHub Actions автоматически:
- Запускает E2E + BDD тесты
- Создает скриншоты и видео
- Загружает артефакты
- Комментирует PR с результатами

---

## 📖 Документация

### Полные руководства

- **[BDD-BEST-PRACTICES.md](../../docs/BDD-BEST-PRACTICES.md)** - Правильный подход к BDD
- **[TESTING-ARCHITECTURE.md](../../docs/TESTING-ARCHITECTURE.md)** - Архитектура системы
- **[E2E-TESTING-GUIDE.md](../../docs/E2E-TESTING-GUIDE.md)** - Полное руководство
- **[scripts/test-automation/README.md](../../scripts/test-automation/README.md)** - Автоматизация

### Быстрые справки

- **[IMPLEMENTATION-COMPLETE.md](./IMPLEMENTATION-COMPLETE.md)** - Статус и статистика
- **[REFACTORING-COMPLETED.md](./REFACTORING-COMPLETED.md)** - История рефакторинга

---

## 🏗️ Архитектура

### Слои системы

```
┌─────────────────────────────────┐
│   BUSINESS LAYER                │
│   *.feature (Gherkin)          │  ← QA пишет вручную
│   - Бизнес-сценарии            │
└─────────────────────────────────┘
              ↓
┌─────────────────────────────────┐
│   STEP DEFINITIONS              │
│   steps.ts | generated-steps.ts│  ← Частично автоген
│   - Технические шаги            │
└─────────────────────────────────┘
              ↓
┌─────────────────────────────────┐
│   COMPONENTS                    │
│   *.tsx (React)                │  ← Dev пишет + data-testid
│   - UI реализация               │
└─────────────────────────────────┘
```

### Правила

**✅ ЧТО МОЖНО автоматически генерировать:**
- Step definitions из `data-testid`
- Базовые технические шаги
- Тесты дизайн-токенов

**❌ ЧТО НЕЛЬЗЯ автоматически генерировать:**
- Feature файлы (.feature)
- Бизнес-сценарии
- Кастомная логика

---

## 🎯 Best Practices

### 1. Используйте data-testid

```tsx
// ✅ Хорошо
<button data-testid="create-test-button">
  Создать тест
</button>

// ❌ Плохо
<button className="btn btn-primary">
  Создать тест
</button>
```

### 2. Пишите читаемые сценарии

```gherkin
# ✅ Хорошо - описывает бизнес-процесс
Сценарий: Создание теста
  Когда преподаватель создает тест "Орфография"
  Тогда тест появляется в списке доступных

# ❌ Плохо - технические детали
Сценарий: Клик по кнопке
  Когда кликаю на data-testid="submit"
  Тогда вызывается handleSubmit()
```

### 3. Используйте теги

```gherkin
@критичный @навигация
Сценарий: Важный тест
  ...

# Запуск только критичных
npx playwright test --grep @критичный
```

---

## 🐛 Troubleshooting

### Тесты не находят элементы

```bash
# 1. Убедитесь что dev server запущен
npm run dev

# 2. Проверьте baseURL в playwright.config.ts
# baseURL: 'http://localhost:3000'

# 3. Запустите в headed mode для отладки
npm run test:bdd:headed
```

### Missing step definitions

```bash
# 1. Регенерируйте тесты
npx bddgen

# 2. Проверьте playwright.config.ts
# steps: ['tests/e2e/steps.ts', 'tests/e2e/generated-steps.ts']

# 3. Добавьте недостающие шаги в steps.ts
```

### Ошибки автогенерации

```bash
# Проверьте что компоненты содержат data-testid
grep -r "data-testid" src/components/

# Запустите автогенерацию
npm run update:tests

# Проверьте generated-steps.ts
cat tests/e2e/generated-steps.ts
```

---

## 📊 Статистика

### Покрытие тестами

- **13 бизнес-сценариев** (Gherkin)
- **82 step definitions** реализовано
- **6 категорий** тестов (навигация, группы, студенты, тесты, UI, статистика)
- **2 feature файла** (teacher-workflow, test-management)

### Документация

- **4 больших гайда** (1500+ строк)
- **3 архитектурных документа** (900+ строк)
- **2 automation скрипта** (450+ строк)

### Автоматизация

- **2 GitHub Actions workflows** (e2e-tests, visual-regression)
- **1 Git hook** (pre-commit)
- **16 npm команд** для тестирования

---

## 🚀 Что дальше?

### Week 5 - Advanced Features (опционально)

1. **Performance Testing**
   ```bash
   npm install --save-dev lighthouse
   ```
   - Lighthouse CI
   - Core Web Vitals
   - Performance budgets

2. **Accessibility Testing**
   ```bash
   npm install --save-dev @axe-core/playwright
   ```
   - WCAG compliance
   - a11y автоматизация

3. **API Testing**
   - E2E + API интеграция
   - Contract testing
   - Mock servers

4. **Component Testing**
   ```bash
   npm install --save-dev @playwright/experimental-ct-react
   ```
   - Изолированные тесты компонентов
   - Storybook интеграция

---

## 👥 Для команды

### Для QA
1. Читай `docs/BDD-BEST-PRACTICES.md`
2. Смотри примеры в `*.feature` файлах
3. Пиши новые сценарии
4. Запускай `npm run test:bdd:ui`

### Для Developers
1. Читай `docs/TESTING-ARCHITECTURE.md`
2. Добавляй `data-testid` в компоненты
3. Запускай `npm run update:tests`
4. Проверяй `npm run test:bdd`

---

## 📞 Поддержка

**Проблемы?** Смотри:
- [Troubleshooting](#troubleshooting)
- [E2E-TESTING-GUIDE.md](../../docs/E2E-TESTING-GUIDE.md)
- [TESTING-ARCHITECTURE.md](../../docs/TESTING-ARCHITECTURE.md)

**Вопросы по архитектуре?**
- Tech Lead → Архитектура
- QA Lead → Сценарии
- DevOps → CI/CD

---

**Версия:** 1.0.0  
**Статус:** ✅ Production Ready  
**Последнее обновление:** 2025-10-09

🎉 **Счастливого тестирования!**
