# 🏗️ Testing Architecture - Контракт системы тестирования

## 📐 Архитектурные принципы

### **Разделение ответственности**

```
┌─────────────────────────────────────────────────────────┐
│                    BUSINESS LAYER                        │
│  (Что тестируем - бизнес-логика)                        │
│                                                           │
│  tests/e2e/*.feature                                     │
│  - Gherkin сценарии на русском                          │
│  - Пишутся вручную QA/аналитиками                       │
│  - Описывают пользовательские пути                      │
└─────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────┐
│                    STEP DEFINITIONS                      │
│  (Как тестируем - техническая реализация)              │
│                                                           │
│  tests/e2e/steps.ts         │  tests/e2e/generated-steps.ts│
│  - Ручные шаги              │  - Автогенерация             │
│  - Бизнес-логика            │  - Базовые действия          │
│  - Кастомная реализация     │  - Из data-testid            │
└─────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────┐
│                    COMPONENTS                            │
│  (Что реализуем - UI элементы)                          │
│                                                           │
│  src/components/**/*.tsx                                 │
│  - React компоненты                                      │
│  - data-testid атрибуты                                  │
│  - UI реализация                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 📁 Структура файлов

```
tests/e2e/
├── 📄 *.feature                    # БИЗНЕС-СЛОЙ
│   ├── user-journey.feature        # Сценарии навигации, UI
│   ├── test-creation.feature       # Создание/редактирование тестов
│   └── [future].feature            # Новые сценарии
│
├── 📝 steps.ts                     # РУЧНЫЕ ШАГИ
│   └── Кастомная бизнес-логика
│       - Сложные проверки
│       - API взаимодействия
│       - Специфичные для проекта шаги
│
├── 🤖 generated-steps.ts           # АВТОГЕНЕРАЦИЯ
│   └── Базовые технические шаги
│       - Клики по элементам
│       - Проверка видимости
│       - Заполнение полей
│       - ⚠️ НЕ редактировать вручную!
│
├── 📦 page-objects/                # PAGE OBJECTS (опционально)
│   └── BasePage.ts
│
├── 🎨 visual-regression.spec.ts   # ВИЗУАЛЬНОЕ СРАВНЕНИЕ
└── 🎯 design-tokens.spec.ts       # ТЕСТЫ ТОКЕНОВ (автоген)
```

---

## 🎭 Роли и ответственность

### 1. **QA Engineer / Business Analyst**

**Отвечает за:**
- ✍️ Написание `.feature` файлов
- 📋 Описание бизнес-сценариев
- 🔍 Определение граничных случаев
- 📊 Приоритизация тестов

**НЕ делает:**
- ❌ Не пишет код step definitions
- ❌ Не модифицирует компоненты
- ❌ Не меняет технические детали

**Пример работы:**
```gherkin
Сценарий: Создание теста с заданиями
  Допустим преподаватель открыл форму создания теста
  Когда преподаватель заполняет название "Контрольная работа №1"
  И выбирает 5 заданий типа "множественный выбор"
  И выбирает 3 задания типа "открытый ответ"
  Тогда тест создается с 8 заданиями
  И отображается в списке доступных тестов
```

---

### 2. **Frontend Developer**

**Отвечает за:**
- 🎨 Реализацию UI компонентов
- 🏷️ Добавление `data-testid` атрибутов
- 🔧 Реализацию кастомных шагов в `steps.ts`
- 🐛 Исправление упавших тестов

**НЕ делает:**
- ❌ Не пишет бизнес-сценарии (.feature)
- ❌ Не редактирует `generated-steps.ts`

**Пример работы:**
```tsx
// Компонент с data-testid
<button data-testid="create-test-button">
  Создать тест
</button>

// Кастомный шаг в steps.ts
Then('тест создается с {int} заданиями', async ({ page }, count) => {
  const testCard = page.locator('[data-testid="test-card"]');
  const taskCount = await testCard.locator('.task-item').count();
  expect(taskCount).toBe(count);
});
```

---

### 3. **Automation System**

**Отвечает за:**
- 🤖 Сканирование компонентов
- ⚙️ Генерацию `generated-steps.ts`
- 🔄 Синхронизацию дизайн-токенов
- 📊 Создание `design-tokens.spec.ts`

**Запускается:**
```bash
npm run update:tests  # Генерация step definitions
npm run sync:tokens   # Синхронизация токенов
```

---

## 📜 Naming Conventions

### Feature файлы

```gherkin
# Формат: [область]-[действие].feature
user-journey.feature       # Навигация пользователя
test-creation.feature      # Создание тестов
student-enrollment.feature # Запись студентов
grade-management.feature   # Управление оценками
```

### Gherkin шаги

**Given (Предусловия):**
```gherkin
Given('пользователь авторизован как {string}')
Given('открыта форма создания теста')
Given('в системе есть {int} активных теста')
```

**When (Действия):**
```gherkin
When('пользователь кликает на {string}')
When('заполняет поле {string} значением {string}')
When('выбирает {int} заданий типа {string}')
```

**Then (Проверки):**
```gherkin
Then('отображается сообщение {string}')
Then('тест появляется в списке')
Then('система отправляет {int} уведомлений')
```

### data-testid атрибуты

**Формат:** `[компонент]-[элемент]-[действие]`

```tsx
// ✅ Хорошо
<button data-testid="test-form-submit-button">
<input data-testid="course-name-input" />
<div data-testid="task-list-container">

// ❌ Плохо
<button data-testid="btn1">
<input id="nameInput" />  // Только id, без testid
<div className="list">     // Без testid
```

---

## 🔄 Workflow

### Создание нового функционала

```
1. Product Owner/BA пишет User Story
   ↓
2. QA пишет Gherkin сценарий (.feature)
   ↓
3. Developer реализует компонент с data-testid
   ↓
4. npm run update:tests (автогенерация)
   ↓
5. Developer добавляет кастомные шаги (если нужно)
   ↓
6. npm run test:bdd (локальный запуск)
   ↓
7. git commit → Pre-commit hook → Автообновление
   ↓
8. git push → CI/CD → Тесты в GitHub Actions
   ↓
9. Результаты в PR комментариях
```

### Изменение существующего функционала

```
1. Developer меняет компонент
   ↓
2. Обновляет data-testid (если нужно)
   ↓
3. git commit → Pre-commit hook
   ↓
4. Автообновление generated-steps.ts
   ↓
5. QA проверяет .feature файлы
   ↓
6. Обновляет сценарии (если логика изменилась)
   ↓
7. npm run test:bdd → Проверка
```

---

## 🚫 Anti-patterns (Что НЕ делать)

### ❌ Технические детали в Gherkin

```gherkin
# ПЛОХО
Сценарий: Клик по кнопке
  When я кликаю на элемент с data-testid "submit-btn"
  And вызывается функция handleSubmit()
  Then изменяется state.isSubmitted на true

# ХОРОШО
Сценарий: Отправка формы
  When пользователь отправляет форму
  Then форма успешно отправлена
  And пользователь видит подтверждение
```

### ❌ Автогенерация feature файлов

```javascript
// ПЛОХО - НЕ ДЕЛАЙТЕ ТАК!
function generateFeatureFile(component) {
  return `
Сценарий: Тест ${component.name}
  Given компонент отображается
  Then компонент работает
  `;
}
```

### ❌ Ручное редактирование generated-steps.ts

```typescript
// ПЛОХО - файл будет перезаписан!
// tests/e2e/generated-steps.ts
When('кастомная логика', async ({ page }) => {
  // Эта логика пропадет при следующей генерации!
});
```

### ❌ Отсутствие data-testid

```tsx
// ПЛОХО
<button className="submit-btn" onClick={handleSubmit}>
  Submit
</button>

// ХОРОШО
<button 
  data-testid="form-submit-button"
  className="submit-btn" 
  onClick={handleSubmit}
>
  Submit
</button>
```

---

## ✅ Best Practices

### 1. **Один сценарий = Одна цель**

```gherkin
# ✅ ХОРОШО
Сценарий: Создание теста
  When преподаватель создает тест
  Then тест создается успешно

Сценарий: Редактирование теста
  When преподаватель редактирует тест
  Then изменения сохраняются

# ❌ ПЛОХО
Сценарий: Работа с тестами
  When создается тест
  And редактируется тест
  And удаляется тест
  And восстанавливается тест
```

### 2. **DRY в step definitions**

```typescript
// ✅ ХОРОШО - переиспользуемый helper
async function clickButton(page: Page, testId: string) {
  await page.locator(`[data-testid="${testId}"]`).click();
}

When('пользователь кликает на {string}', async ({ page }, buttonName) => {
  await clickButton(page, buttonName);
});

// ❌ ПЛОХО - дублирование кода
When('кликает на submit', async ({ page }) => {
  await page.locator('[data-testid="submit"]').click();
});

When('кликает на cancel', async ({ page }) => {
  await page.locator('[data-testid="cancel"]').click();
});
```

### 3. **Используйте Page Objects для сложной логики**

```typescript
// tests/e2e/page-objects/TestFormPage.ts
export class TestFormPage {
  constructor(private page: Page) {}

  async fillTestName(name: string) {
    await this.page.locator('[data-testid="test-name-input"]').fill(name);
  }

  async selectTaskType(type: string, count: number) {
    await this.page.locator(`[data-testid="task-type-${type}"]`).click();
    await this.page.locator('[data-testid="count-input"]').fill(String(count));
  }

  async submit() {
    await this.page.locator('[data-testid="submit-button"]').click();
  }
}

// tests/e2e/steps.ts
When('преподаватель создает тест {string}', async ({ page }, testName) => {
  const formPage = new TestFormPage(page);
  await formPage.fillTestName(testName);
  await formPage.submit();
});
```

---

## 🔒 Контракт API

### generated-steps.ts

**Генерируется из:**
- `data-testid` атрибутов
- `aria-label` атрибутов (опционально)

**Формат шагов:**
```typescript
// Проверка видимости
Given('элемент {название} отображается', async ({ page }) => {
  await expect(page.locator('[data-testid="..."]')).toBeVisible();
});

// Клик
When('пользователь кликает на {название}', async ({ page }) => {
  await page.locator('[data-testid="..."]').click();
});

// Проверка состояния
Then('{название} активен', async ({ page }) => {
  await expect(page.locator('[data-testid="..."]')).toBeEnabled();
});
```

### steps.ts

**Содержит:**
- Комплексные бизнес-сценарии
- API взаимодействия
- Проверки данных
- Работу с несколькими компонентами

**Пример:**
```typescript
Then('тест создается с {int} заданиями', async ({ page }, count) => {
  // Комплексная проверка
  const testCard = page.locator('[data-testid="test-card"]');
  await expect(testCard).toBeVisible();
  
  const tasks = testCard.locator('.task-item');
  expect(await tasks.count()).toBe(count);
  
  // Проверка через API
  const response = await page.request.get('/api/tests/latest');
  const data = await response.json();
  expect(data.taskCount).toBe(count);
});
```

---

## 📊 Метрики качества

### Coverage (Покрытие)

- **Критичные пути:** 100% покрытия Gherkin сценариями
- **Второстепенные:** >80% покрытия
- **Edge cases:** Документированы в сценариях

### Performance

- **E2E тест:** < 30 секунд
- **BDD сценарий:** < 60 секунд
- **Full suite:** < 10 минут

### Maintenance

- **Упавший тест:** Исправить в течение 24ч
- **Новый функционал:** Тесты до merge в main
- **Рефакторинг:** Обновить сценарии одновременно

---

## 🎓 Обучение команды

### Для новых QA

1. Прочитать `BDD-BEST-PRACTICES.md`
2. Изучить существующие `.feature` файлы
3. Написать 1-2 простых сценария
4. Review от senior QA

### Для новых Developers

1. Прочитать этот документ
2. Добавить `data-testid` в 1-2 компонента
3. Запустить `npm run update:tests`
4. Написать 1 кастомный шаг

---

## 🔄 Версионирование

**Контракт версии:** 1.0.0

**История изменений:**
- v1.0.0 (2025-10-09): Первая версия архитектуры

**Обратная совместимость:**
- Старые `.feature` файлы работают
- `generated-steps.ts` регенерируется автоматически
- Breaking changes требуют обновления major версии

---

## 📞 Контакты и поддержка

**Вопросы по:**
- Gherkin сценариям → QA Lead
- Step definitions → Tech Lead
- CI/CD → DevOps
- Архитектуре → Tech Lead

**Документация:**
- `BDD-BEST-PRACTICES.md` - Лучшие практики BDD
- `E2E-TESTING-GUIDE.md` - Руководство по E2E
- `scripts/test-automation/README.md` - Автоматизация

---

**Этот контракт - живой документ. При изменении архитектуры обновляйте его!** 📝
