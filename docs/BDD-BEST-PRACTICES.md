# 🎯 BDD Best Practices: Правильный подход к Gherkin

## ⚠️ Критическое различие

### ❌ ЧТО НЕЛЬЗЯ АВТОМАТИЧЕСКИ ГЕНЕРИРОВАТЬ

**Gherkin Feature файлы (.feature) - БИЗНЕС-ЛОГИКА**

```gherkin
# ❌ ПЛОХО - Автогенерация
Сценарий: Базовая функциональность Button
  Допустим компонент Button отображается
  Тогда Button корректно отображается
```

**Почему плохо:**
- Нет бизнес-смысла
- Не описывает пользовательский сценарий
- Не понятно что тестируется
- Бесполезно для команды

```gherkin
# ✅ ХОРОШО - Написано вручную
Сценарий: Создание заявки на обучение
  Допустим преподаватель заполнил форму заявки
  И указал название курса "Русский язык 100"
  И выбрал дату начала "15 января 2025"
  Когда преподаватель нажимает "Создать заявку"
  Тогда заявка появляется в списке активных
  И студентам отправляются приглашения
  И в календаре создается событие
```

**Почему хорошо:**
- Описывает реальный бизнес-процесс
- Понятно QA, разработчикам, менеджерам
- Документирует поведение системы
- Помогает найти граничные случаи

---

### ✅ ЧТО МОЖНО АВТОМАТИЧЕСКИ ГЕНЕРИРОВАТЬ

**Step Definitions (generated-steps.ts) - ТЕХНИЧЕСКИЕ ДЕТАЛИ**

```typescript
// ✅ Автогенерация из data-testid="submit-button"
When('пользователь нажимает {string}', async ({ page }, buttonName) => {
  await page.locator(`[data-testid="${buttonName}"]`).click();
});

// ✅ Автогенерация из data-testid="course-name-input"
When('указал название курса {string}', async ({ page }, courseName) => {
  await page.locator('[data-testid="course-name-input"]').fill(courseName);
});

// ✅ Автогенерация из data-testid="active-applications-list"
Then('заявка появляется в списке активных', async ({ page }) => {
  const list = page.locator('[data-testid="active-applications-list"]');
  await expect(list).toBeVisible();
});
```

**Почему это полезно:**
- Избавляет от рутинного написания селекторов
- Автоматически обновляется при изменении компонентов
- Поддерживает актуальность `data-testid`
- Создает "библиотеку" переиспользуемых шагов

---

## 🏗️ Правильная структура проекта

```
tests/e2e/
├── *.feature                      # ✍️  Пишутся ВРУЧНУЮ - бизнес-логика
│   ├── user-journey.feature       # Сценарии пользовательского пути
│   ├── course-creation.feature    # Создание курса
│   └── student-enrollment.feature # Запись студентов
│
├── steps.ts                       # ✍️  Пишутся ВРУЧНУЮ - специфическая логика
│   └── Кастомные бизнес-шаги, которые нужны только здесь
│
└── generated-steps.ts             # 🤖 АВТОГЕНЕРАЦИЯ - технические детали
    └── Базовые шаги из data-testid атрибутов
```

---

## 📝 Workflow: Как это работает вместе

### Шаг 1: Разработчик добавляет data-testid

```tsx
// src/components/CourseForm.tsx
export function CourseForm() {
  return (
    <form>
      <input 
        data-testid="course-name-input"
        placeholder="Название курса"
      />
      <input 
        data-testid="start-date-input"
        type="date"
      />
      <button 
        data-testid="create-course-button"
        type="submit"
      >
        Создать курс
      </button>
    </form>
  );
}
```

### Шаг 2: Автогенерация технических шагов

```bash
npm run update:tests
```

Создается `generated-steps.ts`:
```typescript
// Автоматически созданы базовые шаги
Given('элемент course name input отображается', async ({ page }) => {
  await expect(page.locator('[data-testid="course-name-input"]')).toBeVisible();
});

When('пользователь кликает на create course button', async ({ page }) => {
  await page.locator('[data-testid="create-course-button"]').click();
});
```

### Шаг 3: QA/Аналитик пишет бизнес-сценарий

```gherkin
# tests/e2e/course-creation.feature
# language: ru

Функционал: Создание учебного курса
  Как преподаватель
  Я хочу создать новый курс
  Чтобы начать обучение студентов

  Сценарий: Успешное создание курса
    Допустим преподаватель открыл форму создания курса
    И элемент course name input отображается
    Когда преподаватель вводит название "Русский 100"
    И выбирает дату начала "15.01.2025"
    И пользователь кликает на create course button
    Тогда курс появляется в списке курсов
    И студентам отправлены уведомления

  Сценарий: Валидация обязательных полей
    Допустим преподаватель открыл форму создания курса
    Когда преподаватель оставляет название пустым
    И пользователь кликает на create course button
    Тогда отображается ошибка "Название обязательно"
    И курс не создается
```

### Шаг 4: При необходимости - кастомные шаги

Если автогенерированных шагов не хватает, добавляете в `steps.ts`:

```typescript
// tests/e2e/steps.ts
import { createBdd } from 'playwright-bdd';

const { Given, When, Then } = createBdd();

// Кастомная бизнес-логика
Then('курс появляется в списке курсов', async ({ page }) => {
  const courseList = page.locator('[data-testid="courses-list"]');
  const newCourse = courseList.locator('text="Русский 100"');
  await expect(newCourse).toBeVisible();
});

Then('студентам отправлены уведомления', async ({ page }) => {
  // Проверка через API или mock
  const notifications = await page.request.get('/api/notifications');
  expect(notifications.ok()).toBeTruthy();
});
```

---

## 🎯 Правила написания хороших сценариев

### 1. Описывайте ПОВЕДЕНИЕ, а не реализацию

```gherkin
# ❌ ПЛОХО - слишком технично
Сценарий: Клик по кнопке submit
  Когда пользователь кликает на кнопку с id "submit-btn"
  Тогда вызывается функция handleSubmit()

# ✅ ХОРОШО - описывает бизнес-процесс
Сценарий: Отправка заявки
  Когда пользователь отправляет заявку
  Тогда заявка регистрируется в системе
```

### 2. Пишите с точки зрения ПОЛЬЗОВАТЕЛЯ

```gherkin
# ❌ ПЛОХО - технические детали
Сценарий: POST запрос к /api/courses
  Когда отправляется POST с телом {...}
  Тогда возвращается 201 статус

# ✅ ХОРОШО - пользовательский опыт
Сценарий: Создание курса
  Когда преподаватель создает курс
  Тогда курс доступен для записи студентов
```

### 3. Один сценарий = Одна бизнес-цель

```gherkin
# ❌ ПЛОХО - проверяет всё подряд
Сценарий: Тестирование формы
  И проверяется валидация
  И проверяется отправка
  И проверяется редактирование
  И проверяется удаление

# ✅ ХОРОШО - фокус на одной цели
Сценарий: Валидация обязательных полей
  Когда поля оставлены пустыми
  Тогда отображаются ошибки валидации
```

### 4. Используйте Examples для вариаций

```gherkin
# ✅ ХОРОШО - параметризация
Сценарий: Валидация формата email
  Когда пользователь вводит email "<email>"
  Тогда отображается <результат>

  Примеры:
    | email              | результат          |
    | valid@email.com    | форма принимается  |
    | invalid-email      | ошибка валидации   |
    | @missing.com       | ошибка валидации   |
```

---

## 🔄 Практика: Аннотации в коде?

**Да, существует практика аннотаций:**

```typescript
/**
 * @gherkin-step When('пользователь создает курс')
 * @gherkin-step Then('курс создан успешно')
 */
export async function createCourse(data: CourseData) {
  // ...
}
```

**Но это плохая практика для BDD, потому что:**
- Смешивает бизнес-логику с кодом
- Аннотации устаревают
- Не читаемо для не-разработчиков
- Нарушает принцип разделения ответственности

**Лучше:**
- Feature файлы отдельно (бизнес-слой)
- Step Definitions отдельно (технический слой)
- Компоненты отдельно (реализация)

---

## 📊 Итоговая таблица

| Что | Автогенерация | Кто пишет | Зачем |
|-----|---------------|-----------|-------|
| **Feature файлы** | ❌ НЕТ | QA/Аналитики/Team | Описание бизнес-процессов |
| **generated-steps.ts** | ✅ ДА | Скрипт | Базовые технические действия |
| **steps.ts** | ❌ НЕТ | Разработчики/QA | Специфическая бизнес-логика |
| **Компоненты** | ❌ НЕТ | Разработчики | Реализация UI |

---

## 🎓 Примеры из реального мира

### Плохой пример (всё автоматом)

```gherkin
Сценарий: Button test
  Given компонент Button отображается
  When кликается button
  Then Button работает
```
↑ Что это тестирует? Непонятно!

### Хороший пример (осмысленный)

```gherkin
Сценарий: Студент записывается на курс
  Допустим студент открыл каталог курсов
  И видит доступный курс "Русский 100"
  Когда студент нажимает "Записаться"
  Тогда студент добавлен в список участников
  И получает приветственное письмо
  И видит курс в разделе "Мои курсы"
  
  @критичный @регистрация
```
↑ Понятно всем, описывает реальный процесс!

---

## ✨ Вывод

**Правильный подход к BDD:**

1. **Feature файлы** - Пишутся ВРУЧНУЮ, описывают бизнес
2. **Generated Steps** - Автогенерируются из `data-testid`, технические детали
3. **Custom Steps** - Пишутся вручную для специфической логики
4. **Разделение** - Бизнес-логика отдельно от технической реализации

**Команда выигрывает:**
- QA понимают что тестировать
- Разработчики понимают что реализовывать
- Менеджеры понимают что работает
- Документация всегда актуальна

🎯 **BDD - это про коммуникацию, а не про автоматизацию!**
