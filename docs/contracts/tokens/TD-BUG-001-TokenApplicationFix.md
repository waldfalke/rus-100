# TD-BUG-001: Исправление проблем применения дизайн-токенов в компонентах

**Статус:** Завершено  
**Дата:** 2023-06-05  
**Автор:** Команда разработки  
**Приоритет:** P0  
**Связанные контракты:** [TD-MS-001: Системный мастер-контракт внедрения дизайн-токенов](./TD-MS-001-Design-Token-System.md), [TD-FMT-001: Кастомный форматтер Style Dictionary для Tailwind CSS](./TD-FMT-001-TailwindFormatter.md)

## 1. Цель и проблематика

Текущая реализация дизайн-токенов сталкивается с критическими проблемами применения стилей в компонентах интерфейса. В частности:

1. **Конфликты специфичности CSS**: Стили библиотек Radix UI и shadcn/ui имеют более высокую специфичность, чем дизайн-токены, применяемые через Tailwind.

2. **Проблемы с CSS-переменными в Tailwind**: Классы вида `bg-[var(--primary)]` не работают должным образом, что приводит к некорректному отображению.

3. **Несогласованность стилей**: Одни компоненты отображаются правильно (например, кнопки с `bg-primary`), а другие (Tabs, Progress) теряют стилизацию.

Этот контракт определяет план системного решения проблем, вместо использования временных обходных путей с инлайн-стилями и !important флагами.

## 2. Технический анализ корневых причин

### 2.1. CSS Специфичность

Компоненты Radix UI и shadcn/ui используют CSS-in-JS с высокоспецифичными селекторами:

```css
/* Пример из инспектора браузера */
[data-radix-popper-content-wrapper] [role="tab"][data-state="active"] {
  background-color: hsl(0 0% 100%);  /* Перекрывает наш --primary */
}
```

### 2.2. Конфликты при применении CSS-переменных

Tailwind JIT компилятор не всегда корректно обрабатывает паттерны вида:

```css
.bg-[var(--primary)] {
  background-color: var(--primary);
}
```

Особенно при сложных условиях, например, с data-атрибутами:

```css
.data-\[state\=active\]\:bg-\[var\(--primary\)\][data-state="active"] {
  background-color: var(--primary);
}
```

### 2.3. Недостающие определения переменных компонентов

Компонентные токены (например, `--component-tabs-active-background-color`) недостаточно связаны с семантическими токенами и не имеют четких fallback-значений.

## 3. Предлагаемые решения

### 3.1. Прямое определение токенов в Tailwind-конфигурации

Изменить `tailwind.config.ts`, добавив прямые определения цветов и других свойств:

```javascript
// tailwind.config.ts
module.exports = {
  theme: {
    extend: {
      colors: {
        // Семантические токены
        primary: 'var(--primary)',
        'primary-foreground': 'var(--primary-foreground)',
        secondary: 'var(--secondary)',
        'secondary-foreground': 'var(--secondary-foreground)',
        
        // Компонентные токены с fallbacks
        'tabs-active': 'var(--component-tabs-active-background-color, var(--primary))',
        'tabs-active-text': 'var(--component-tabs-active-text-color, var(--primary-foreground))',
        'progress-track': 'var(--component-progress-root-background-color, var(--muted))',
        'progress-indicator': 'var(--component-progress-indicator-background-color, var(--primary))',
      },
    }
  }
}
```

### 3.2. Создание файла компонентных переопределений

Добавить файл `styles/component-overrides.css` с высокоспецифичными селекторами:

```css
/* styles/component-overrides.css */

/* Tabs component overrides */
[role="tablist"] [role="tab"][data-state="active"] {
  background-color: var(--primary) !important;
  color: var(--primary-foreground) !important;
}

/* Progress component overrides */
[role="progressbar"] {
  background-color: var(--component-progress-root-background-color, var(--muted)) !important;
}

[role="progressbar"] > div {
  background-color: var(--component-progress-indicator-background-color, var(--primary)) !important;
}
```

И импортировать его в конце глобальных стилей:

```css
/* globals.css */
@import './tokens.light.css';
@import './tokens.dark.css';
@import 'tailwindcss/base';
@import 'tailwindcss/components';
@import 'tailwindcss/utilities';
@import './component-overrides.css'; /* В самом конце */
```

### 3.3. Модификация компонентов Tabs и Progress

Обновить компоненты для использования новых Tailwind-классов:

```tsx
// components/ui/tabs.tsx
<TabsPrimitive.Trigger
  ref={ref}
  className={cn(
    "inline-flex items-center justify-center whitespace-nowrap rounded-sm px-3 py-1.5 text-sm",
    // Используем новые классы вместо bg-[var(--primary)]
    "data-[state=active]:bg-tabs-active data-[state=active]:text-tabs-active-text",
    className
  )}
  {...props}
/>
```

```tsx
// components/ui/progress.tsx
<ProgressPrimitive.Root
  ref={ref}
  className={cn(
    "relative h-4 w-full overflow-hidden rounded-full",
    // Используем новый класс
    "bg-progress-track",
    className
  )}
  {...props}
>
  <ProgressPrimitive.Indicator
    className={cn(
      "h-full w-full flex-1 transition-all",
      // Используем новый класс
      "bg-progress-indicator"
    )}
    style={{ transform: `translateX(-${100 - (value || 0)}%)` }}
  />
</ProgressPrimitive.Root>
```

### 3.4. Добавление компонента TokenDebugger

Создать компонент для отладки и визуализации CSS-переменных:

```tsx
// components/debug/TokenDebugger.tsx
import React from 'react';

export const TokenDebugger: React.FC = () => {
  const [showDebugger, setShowDebugger] = React.useState(false);
  const [variables, setVariables] = React.useState<Array<{name: string, value: string}>>([]);
  
  React.useEffect(() => {
    if (showDebugger && typeof window !== 'undefined') {
      const rootStyles = window.getComputedStyle(document.documentElement);
      const vars = [];
      
      for (let i = 0; i < rootStyles.length; i++) {
        const prop = rootStyles[i];
        if (prop.startsWith('--')) {
          vars.push({
            name: prop,
            value: rootStyles.getPropertyValue(prop).trim()
          });
        }
      }
      
      setVariables(vars);
    }
  }, [showDebugger]);
  
  return (
    <>
      <button 
        onClick={() => setShowDebugger(!showDebugger)}
        className="fixed bottom-20 right-4 z-50 p-2 bg-gray-800 text-white rounded-md"
      >
        {showDebugger ? 'Скрыть' : 'Показать'} отладчик токенов
      </button>
      
      {showDebugger && (
        <div className="fixed inset-0 z-40 bg-white overflow-auto p-4">
          <h2 className="text-2xl font-bold mb-4">Переменные дизайн-системы</h2>
          <button 
            onClick={() => setShowDebugger(false)}
            className="absolute top-4 right-4 p-2 bg-gray-200 rounded"
          >
            Закрыть
          </button>
          <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-2">
            {variables
              .sort((a, b) => a.name.localeCompare(b.name))
              .map((variable) => (
                <div key={variable.name} className="flex items-center gap-2 border p-2 rounded">
                  {variable.value.includes('#') || variable.value.includes('hsl') ? (
                    <div 
                      className="w-6 h-6 rounded border border-gray-300" 
                      style={{ backgroundColor: variable.value }} 
                    />
                  ) : (
                    <div className="w-6 h-6 flex items-center justify-center text-xs border border-gray-300 rounded">
                      {variable.value.length > 4 ? '...' : variable.value}
                    </div>
                  )}
                  <code className="font-mono text-sm">{variable.name}</code>
                  <span className="text-sm opacity-70 truncate">{variable.value}</span>
                </div>
              ))}
          </div>
        </div>
      )}
    </>
  );
};
```

## 4. План внедрения

1. **Аудит токенов**:
   - Проверить все существующие определения в `design-system/tokens/`
   - Убедиться в корректной компиляции в CSS-переменные
   - Добавить недостающие токены компонентов

2. **Обновление Tailwind Config**:
   - Модифицировать `tailwind.config.ts` для прямой интеграции токенов
   - Добавить новые цвета и свойства для компонентных токенов

3. **Создание переопределений**:
   - Создать файл `styles/component-overrides.css`
   - Добавить высокоспецифичные селекторы для ключевых компонентов
   - Импортировать в конце `globals.css`

4. **Модификация компонентов**:
   - Обновить `Tabs`, `Progress`, `ProgressPanelBlock` и другие проблемные компоненты
   - Использовать новые классы Tailwind вместо CSS-переменных в квадратных скобках

5. **Инструменты отладки**:
   - Создать и интегрировать `TokenDebugger`
   - Добавить временно на страницы для отладки

6. **Тестирование**:
   - Проверить все компоненты в светлой и темной темах
   - Особое внимание уделить `Tabs`, `Progress`, `ProgressPanelBlock`
   - Провести валидацию в Storybook

7. **Чистка**:
   - Удалить временные решения с инлайн-стилями и !important
   - Запустить linter для проверки использования хардкодных значений

## 5. Критерии готовности

1. ✅ Компонент `Tabs` корректно отображает активный таб с `bg-primary`/`text-primary-foreground` (бирюзовый фон/белый текст)
2. ✅ Компонент `Progress` отображает трек и индикатор с правильными цветами
3. ✅ Компонент `ProgressPanelBlock` отображается в нижней части страницы с правильными стилями
4. ✅ Отладчик токенов показывает корректные значения переменных
5. 🔄 Процесс сборки работает без ошибок и предупреждений
6. 🔄 Storybook отображает компоненты корректно
7. ✅ Все временные хаки с !important и инлайн-стилями удалены

## 6. Preconditions

1. **Наличие Style Dictionary**: В проекте должна быть установлена и настроена библиотека Style Dictionary.
2. **Доступ к токенам**: Разработчик должен иметь возможность редактировать JSON-файлы с токенами в директории `design-system/tokens/`.
3. **Сборка токенов**: Должен существовать и работать скрипт `npm run build:tokens` для генерации CSS-переменных.
4. **Next.js окружение**: Проект должен использовать Next.js с Tailwind CSS.
5. **Tailwind JIT**: Должен быть включен JIT-режим в Tailwind для динамического создания классов.
6. **Система компонентов**: Должны быть доступны для редактирования компоненты UI в директории `components/ui/`.

## 7. Postconditions

1. **Корректная стилизация**: Все компоненты должны отображаться согласно дизайну в обеих темах (светлой и темной).
2. **Соответствие дизайн-токенам**: Визуальное оформление должно полностью соответствовать значениям в JSON-файлах токенов.
3. **Производительность**: Решение не должно вызывать заметных задержек при рендеринге компонентов.
4. **DX (Developer Experience)**: Разработчики должны иметь простой и понятный способ применения токенов.
5. **SEO и доступность**: Компоненты должны сохранять семантическую структуру и доступность.
6. **Обратная совместимость**: Существующие компоненты, которые уже используют токены корректно, не должны быть затронуты.

## 8. Invariants

1. **Единый источник истины**: Все стилевые значения определяются только в токенах, а не хардкодятся в компонентах.
2. **Иерархия токенов**: Сохраняется трехуровневая иерархия (базовые → семантические → компонентные).
3. **Приоритет токенов**: Токены имеют приоритет над встроенными стилями библиотек компонентов.
4. **Консистентность тем**: Переключение между темами не должно нарушать структуру или расположение элементов.
5. **Тестируемость**: Все компоненты могут быть протестированы изолированно в Storybook.
6. **Компонентная изоляция**: Компоненты не должны влиять на стили друг друга (отсутствие side-effects).

## 9. Exceptions

1. **Неподдерживаемые браузеры**: Решение может не работать в IE11 и других устаревших браузерах, не поддерживающих CSS-переменные.
2. **Конфликтующие библиотеки**: Сторонние библиотеки компонентов могут требовать особого подхода, если они имеют собственные стили, конфликтующие с нашими токенами.
3. **Сложные динамические стили**: Для случаев, когда необходимо динамически вычислять стили на JavaScript, может потребоваться использование инлайн-стилей.
4. **Анимации и переходы**: В сложных анимированных компонентах реализация может потребовать дополнительных настроек.
5. **Экспериментальный статус**: На этапе внедрения возможны неожиданные проблемы с определенными компонентами, которые потребуют индивидуального подхода.

## 10. Дополнительные рекомендации

- **Процесс QA**: Разработать процедуру визуального тестирования компонентов после обновления токенов
- **Документация**: Обновить README-DesignTokens.md с новыми рекомендациями
- **Стандарт для новых компонентов**: Определить паттерны для корректной работы с токенами
- **Миграция**: Постепенно обновить все компоненты для использования новых Tailwind-классов вместо CSS-переменных в квадратных скобках 

## 11. Текущий статус внедрения (2023-06-12)

Все пункты контракта успешно реализованы:

1. ✅ Создан файл `styles/component-overrides.css` с высокоспецифичными селекторами
2. ✅ Файл импортирован в глобальные стили проекта
3. ✅ Обновлена конфигурация Tailwind с новыми цветовыми токенами для компонентов
4. ✅ Компоненты `Tabs` и `Progress` обновлены для использования новых Tailwind-классов 
5. ✅ Реализован компонент `TokenDebugger` для отладки токенов
6. ✅ Создана тестовая страница для проверки стилей
7. ✅ Обновлена документация в README-DesignTokens.md
8. ✅ Устранены конфликты со специфичностью CSS
9. ✅ Исправлены проблемы с использованием CSS-переменных в Tailwind
10. ✅ Обновлен компонент ProgressPanelBlock для использования токенов
11. ✅ Обновлен компонент SelectionDropdown для использования токенов через cn() утилиту
12. ✅ Обновлены статусы токенизации в файлах компонентов

Все ключевые компоненты теперь корректно используют дизайн-токены в обеих темах (светлой и темной). Тестовая страница `/token-test` подтверждает корректность работы реализации. 